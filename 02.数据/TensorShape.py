'''学习有关tensorshape相关的知识https://www.tensorflow.org/api_docs/python/tf/TensorShape'''import tensorflow as tf'''A TensorShape表示了一个tensor的shape. 它可能是下面的某种形式    全知shape:知道每个维度的元素个数 e.g. TensorShape([16, 256])    部分知道 shape: 知道维度信息, 不知道每个维度的元素个数 e.g. TensorShape([None, 256])    Unknown shape: 维度和每个维度信息都不知道 e.g. TensorShape(None)'''# 1. as_list# Returns a list of integers or None for each dimension.# 2. assert_has_rank（rank）# Raises an exception if self is not compatible with the given rank.# 3. assert_is_compatible_with(other)# Raises exception if self and other do not represent the same shape.# 4. assert_is_fully_defined# Raises an exception if self is not fully defined in every dimension.# 5. assert_same_rank# Raises an exception if self and other do not have compatible ranks# 6. concatenate(other)# Returns the concatenation of the dimension in self and other# 7. is_compatible_with(other)# 两个tensor是不是兼容，也就是维度上一致# TensorShape(None) 兼容所有shapes# TensorShape([None, None]) 兼容所有的二维shapes,([32, 784]), 也兼容(None).#                           不兼容 TensorShape([None]) or TensorShape([None, None, None]).# TensorShape([32, None]) 兼容 TensorShape([None, None]) and TensorShape(None).#                        不兼容, TensorShape([32]), TensorShape([32, None, 1]) or TensorShape([64, None]).# TensorShape([32, 784]) 兼容 TensorShape([32, None]), TensorShape([None, 784]), TensorShape([None, None]) and TensorShape(None).#                          不兼容, TensorShape([32, 1, 784]) or TensorShape([None]).# 8. is_fully_defined# Returns True iff self is fully defined in every dimension.# 9. merge_with（other）# 10. num_elements# Returns the total number of elements, or none for incomplete shapes.# 11. with_rank(rank)# 基于rank返回shape